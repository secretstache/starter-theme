<header {!! $id !!} {!! $classes !!}>

    @if ($subheadline)
        <h3 class="subheadline">{!! $subheadline !!}</h2>
    @endif
    
    @if ($headline)
        <h2 class="headline">{!! $headline !!}</h2>
    @endif

</header>